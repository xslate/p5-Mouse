# auto-generated shipit config file.
steps = FindVersion, ChangeAllVersions, CheckChangeLog, MakeDist, DistTest, Commit, Tag, UploadCPAN

git.tagpattern = %v
git.push_to = origin

CheckChangeLog.files = Changes
